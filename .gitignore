# Dependencies
node_modules

# Superpowers brainstorm sessions
.superpowers/
.pnp
.pnp.js
# bun.lock (lockfile do gerenciador do projeto) é versionado — builds reproduzíveis.
# Lockfiles de outros gerenciadores ficam ignorados.
bun.lockb
package-lock.json
yarn.lock
pnpm-lock.yaml


# Build outputs
dist
build
*.tsbuildinfo

# Environment variables
.env
.env*.local

# IDEs and editors
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea
*.swp
*.swo
*~
.DS_Store

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Turbo
.turbo
.nx

# Better-T-Stack
.alchemy

# Testing
coverage
.nyc_output

# Misc
*.tgz
.cache
tmp
temp

# Claude Code — artefatos transitórios de sessão
.claude/scheduled_tasks.lock

# Drizzle Kit migrations geradas (DB owned-by-dashboard — schema TS é a fonte de verdade local)
# Triggers/RLS manuais (_triggers.sql, _rls.sql) permanecem versionados — NÃO ignorar o diretório.
packages/db/src/migrations/[0-9]*.sql
packages/db/src/migrations/meta/
